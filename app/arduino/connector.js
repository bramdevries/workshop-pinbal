/*var firmata = require('firmata');

/*
var connector = {
  path : '/dev/tty.usbmodemfd121',
  connect: function() {
    this.board = new firmata.Board(this.path, function(err){
      if (err) { console.log(err); return;}
    });
  }
};

module.exports = connector;*/